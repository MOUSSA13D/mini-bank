# Example environment variables (DO NOT put secrets here)
# Copy this file to .env and fill with your real values locally.

# MongoDB
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-host>.mongodb.net
DB_NAME=mini_bank

# Auth
JWT_SECRET=<generate_a_strong_random_secret>

# Server
# PORT=4000  # optional, defaults to 4000
